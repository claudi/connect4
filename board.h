#include "defs.h"
#include <stdlib.h>

void copyNode(Node *destination, Node *origin);
void copyBoard(Board destination, Board origin);
void makeMove(Node *node, size_t column);

// TODO: return values
